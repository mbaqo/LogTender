[pytest]
minversion = 8.0
testpaths =
    app/tests
python_files = test_*.py
addopts =
    -q
    --strict-markers
filterwarnings =
    error
    ignore:.*pkg_resources is deprecated.*:DeprecationWarning
